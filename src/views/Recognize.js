import React from "react";

const Recognize = () => (
  <div className="flex items-center justify-center min-h-screen">
    <div className="text-7xl">Recognizing...</div>
  </div>
);

export default Recognize;
